<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <%- include('partials/nav') %>

  <h1><%= category %> Menu</h1>

  <ul>
    <% menu.forEach(item => { %>
      <li>
        <strong><%= item.name %> <%= item.rating === 5 ? '⭐' : '' %></strong>
        <p>Price: $<%= item.price %></p>
        <p>Rating: <%= item.rating %>/5</p>
        <p><%= item.details %></p>
      </li>
    <% }); %>
  </ul>
</body>
</html>
